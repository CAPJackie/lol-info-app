services:
  verify:
    image: lol-info-next:verify
    build:
      context: .
      target: verify

  app:
    image: lol-info-next:prod
    build:
      context: .
      target: runner
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "3000:3000"
